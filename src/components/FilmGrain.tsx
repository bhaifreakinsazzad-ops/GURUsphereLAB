/**
 * Filmic noise overlay — sits above everything, ignores pointer events.
 * Cohesion layer: 6% opacity SVG noise blended overlay.
 */
const FilmGrain = () => <div className="film-grain" aria-hidden="true" />;

export default FilmGrain;
